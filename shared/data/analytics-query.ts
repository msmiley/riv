//
// Analytics query class
//
import { RivData } from '../base/riv-data.ts';

export class AnalyticsQuery extends RivData {
  constructor(obj = {}, defaults = {
  }) {
    super(obj, defaults);
  }
}
